# Roadmap

## Fase 1
- [x] Arquitetura conceitual

## Fase 2
- [x] Arquitetura técnica
  - Consolidação documental: Etapas 1-7 concluídas — Fase 2 encerrada

## Fase 3
- [x] Modelagem do banco
  - Modelagem física concluída: modelo lógico, convenções físicas e modelagem por entidade; schema PostgreSQL implementado (tabelas, constraints, índices); `05_views.sql` e `07_triggers.sql` adiados, `06_functions.sql` descartado

## Fase 4
- [x] Backend
  - Fase encerrada em 2026-08-19 — ver `freeze-fase-4.md`. 24 módulos de domínio implementados em Clean Architecture, incorporando as decisões #12-#40. **Escopo explicitamente adiado para a Fase 5** (decisão #41): implementação de T3/A2/A4/A5/A8 (autenticação, RBAC + escopo por Empresa, checagem de permissão, auditoria automática, reautenticação de IA) — ver `pendencias.md`, Seção 7, itens S1-S5. Até essa implementação, todos os endpoints permanecem sem autenticação
  - T1 (linguagem/framework/gerenciador de dependências/ORM) definida: Java 21 LTS + Spring Boot + Maven + Hibernate/JPA — ver `decisions.md`, decisão #12
  - A1 (estilo de arquitetura de software) definida: Clean Architecture, com Hexagonal/Ports & Adapters tratado como a mesma alternativa arquitetural — ver `decisions.md`, decisão #13
  - T3 (estratégia de autenticação) definida: autenticação própria, Spring Security + JWT, hash de senha com Argon2 (bcrypt só como alternativa documentada), sem provedor externo — ver `decisions.md`, decisão #14
  - A4 (mecanismo dos dois pontos de checagem de permissão) definida: Spring Security Method Security (ponto por ação) + Hibernate/JPA Filters via `@FilterDef`/`@Filter` (ponto por escopo de Empresa) — ver `decisions.md`, decisão #15
  - A2 (modelo de permissões de usuário) definida: RBAC + escopo por Empresa — ver `decisions.md`, decisão #16
  - A3 (cruzamento entre papel de usuário e níveis de confirmação da IA) definida: Papel → nível máximo — ver `decisions.md`, decisão #17
  - A5 (mecanismo técnico de auditoria automática) definida: aspecto customizado (Spring AOP, `@Around`), interceptando cada caso de uso de escrita e gravando em `LOG_AUDITORIA` na mesma transação da escrita de negócio — ver `decisions.md`, decisão #18
  - Mecanismo do módulo de consulta compartilhado (`application/consultas-financeiras`) definido: serviço de aplicação (Application Service/Facade) sobre Spring Data JPA — decisão técnica independente, sem código de pendência associado — ver `decisions.md`, decisão #19. Desenho arquitetural do módulo (responsabilidade, consumidores, categorias de consulta, contrato) definido — ver `decisions.md`, decisão #35; A6 resolvida. Métodos concretos e fórmulas internas permanecem para quando cada funcionalidade for especificada
  - B4 (implementação técnica do vínculo genérico de auditoria) definida: referência polimórfica (`entidade_tipo`/`entidade_id`), sem FK nativa do banco, validação de existência na camada de aplicação — ver `decisions.md`, decisão #20. Estratégia de índice (B3) resolvida em nível arquitetural — ver `decisions.md`, decisão #37; indexação das colunas de referência genérica segue o mesmo critério objetivo
  - D7 (vínculo direto `LANÇAMENTO_FINANCEIRO` ↔ `EMPRESA`) definida: `empresa_id` obrigatório desde a criação em `LANÇAMENTO_FINANCEIRO`, com validação de consistência com `VEÍCULO.empresa_id` na camada de aplicação; `AÇÃO_PROPOSTA_IA` ganha `empresa_id`/status "Aguardando Empresa" — ver `decisions.md`, decisão #21. Demais decisões da Fase 4 permanecem em aberto — ver `pendencias.md`

## Fase 5
- [ ] Frontend
  - T2 (framework de frontend) definida: **React + Vite + TypeScript**, aplicação desacoplada consumindo a API REST do backend — ver `decisions.md`, decisão #42
  - T14 (hospedagem do frontend) definida: mesma plataforma PaaS do backend (T4), sem infraestrutura separada — ver `decisions.md`, decisão #43
  - T7 (roteamento e navegação) definida: React Router — ver `decisions.md`, decisão #44
  - T8 (testes) definida: Vitest + React Testing Library, sem Jest — ver `decisions.md`, decisão #45
  - T9 (cliente HTTP) definida: axios, camada compartilhada `src/api/client` — ver `decisions.md`, decisão #46
  - T10 (gerenciamento de estado) definida: TanStack Query (servidor) + Context API (UI local), sem Redux — ver `decisions.md`, decisão #47
  - T11 (autenticação no cliente) definida: cookie httpOnly, sem localStorage/sessionStorage — impõe requisito a S1 (backend) — ver `decisions.md`, decisão #48
  - T12 (UI e estilo) definida: Tailwind CSS + shadcn/ui; DataGrid deliberadamente adiado — ver `decisions.md`, decisão #49
  - T13 (estrutura de pastas e padrão arquitetural) definida: por feature/módulo de domínio (`app/`, `features/`, `shared/`, `routes/`), sem Clean Architecture completa — ver `decisions.md`, decisão #50
  - **Marco**: com T13, todas as nove decisões constituintes do Frontend (T2, T7-T14) encerradas — arquitetura técnica completa do Frontend concluída, ver `architecture/arquitetura-tecnica-frontend.md`
  - Herda S1-S5 (`pendencias.md`, Seção 7) como dependência formal — login, rotas protegidas e UI condicionada a papel/Empresa dependem da implementação de autenticação/autorização/auditoria, adiada da Fase 4 (decisão #41)
  - Implementação iniciada: projeto React + Vite + TypeScript criado (`frontend/`), com Tailwind CSS, shadcn/ui, React Router, TanStack Query e cliente axios (`shared/api/client`) configurados; estrutura de pastas de T13 (`app/`, `features/`, `shared/`, `routes/`) criada; infraestrutura compartilhada implementada (providers, layout base, tema, tratamento centralizado de `ApiError`, loading global). Build, testes (Vitest + RTL) e lint passando.
  - **Cadastros Base completos**: `empresa/`, `fornecedor/`, `cliente/`, `categoria/`, `conta-bancaria/`, `cartao-credito/` (só o cadastro) e `usuario/` (nível básico) implementados — CRUD, validação (react-hook-form + zod), tratamento de erro via `shared/api`, testes (36/36 passando). Componentes compartilhados extraídos por reuso real: `shared/components/crud/NomeUnicoForm`/`NomeUnicoTable` (Empresa/Fornecedor/Cliente), `shared/hooks/useEmpresaOptions`/`useContaBancariaOptions` (referência cross-feature)
  - **Cadeia financeira central implementada**: `lancamento-financeiro/` (CRUD + cancelamento) e `liquidacao-financeira/` (criação + detalhe, sem edição — imutável, D12; array `aplicacoes` embutido na criação, sem tela própria de Aplicação de Liquidação — sem endpoint HTTP no backend). Testes (47/47 passando). Achado: Obra e Frota já têm API REST completa no backend, mas ficaram de fora do Frontend nesse lote (fora do escopo pedido; campos opcionais em Lançamento) — implementadas no lote seguinte
  - **Obra e Frota implementados**: `obra/` (CRUD + transição de status, D9) e `frota/` (CRUD, `tipo` com 8 valores fechados, D10; rota `/veiculos`). `lancamento-financeiro/` atualizada para incluir seleção de Obra e Veículo (filtrado por Empresa). Testes (63/63 passando)
  - Ordem de implementação revisada (`arquitetura-tecnica-frontend.md`, Seção 11): login/`AuthProvider` adiados para quando S1-S5 começarem a ser implementados no backend — sem dependência técnica real que os exigisse antes; primeira feature funcional implementada seguindo a ordem do backend, começando por Empresa
  - Módulo **Empresa** implementado (`features/empresa/`) — CRUD completo (listar, criar, editar), validação client-side (react-hook-form + zod), integração com `ApiError` do backend, testes (Vitest + RTL). Ainda sem RBAC/escopo por Empresa na UI (depende de S2/S3)
  - **Conciliação Bancária implementada**: `conciliacao-bancaria/` — Movimentação Bancária (importação de extrato em lote + reclassificação, único campo mutável), Transferência Interna (criação + listagem, sem edição) e Vínculo Conciliação (sem tela própria — vínculo 1:1 obrigatório com Movimentação, gerado automaticamente no backend; ações de confirmar/marcar divergente/marcar sem correspondência/vincular manualmente/rodar sugestão automática embutidas na tabela de Movimentação, seguindo exatamente as guardas de `VinculoConciliacaoService`). Novo hook compartilhado `useLiquidacaoOptions`. Testes (83/83 no total)
  - **Cartão de Crédito (fluxo financeiro) implementado**: `compra-cartao/` (CRUD, `cartaoId`/`valor`/`numeroParcelas` imutáveis após criar; correção de categoria/obra/veículo propaga para Lançamentos já gerados, D5), `fatura/` (sem criação/edição genérica — só `fecharCiclo`, que já nasce com os totais congelados, e `pagar`, que cria Liquidação em cascata com Movimentação Bancária/Vínculo automáticos) e `parcela/` (só leitura + ação manual `gerar-lancamento`, sem agendador automático no projeto; filtro por Compra Cartão sincronizado com a URL). Novos hooks compartilhados `useCartaoCreditoOptions`/`useCompraCartaoOptions`/`useParcelaOptions`. Testes (100/100 no total)
  - **Financiamentos e Consórcios implementado**: `financiamento-consorcio/` — CRUD de Contrato Financeiro (campos imutáveis após criar: `tipo`/`empresaId`/`contaBancariaId`/`valorContratado`/`numeroParcelas`/`dataVencimentoPrimeiraParcela`) + ação dedicada `contemplar` (transição Não→Sim, exclusiva de Consórcio); campos condicionais por `tipo` (`taxa` × `grupoCota`/`contemplado`/`veiculoId`) exigidos só no modo criar. `features/parcela/` estendida para o filtro real `?contratoFinanceiroId=` (reuso direto, sem duplicar a listagem). Novo hook compartilhado `useContratoFinanceiroOptions`. Testes (111/111 no total)
  - **Balanço implementado**: `balanco/` — só leitura/agregação, um único endpoint (`GET /api/balanco?empresaId=`), Realizado (fração já coberta por Liquidação/Aplicação) e Projetado (Lançamentos Aberto + Parcial + Pago, exceto Cancelado). Primeira tela "dashboard" do Frontend — `shared/components/ui/card` (shadcn/ui) usado pela primeira vez, componente `ResultadoCard` extraído por reuso real (6 instâncias). Testes (116/116 no total)
  - **Ajuste Financeiro implementado**: `ajuste-financeiro/` — só `criar` (sem `atualizar`/`excluir`, imutável desde a criação, D13); vincula dois Lançamentos já existentes (o Lançamento de ajuste é criado separadamente, via `lancamento-financeiro/`, antes de formalizar o vínculo aqui). Reusa `useLancamentoOptions` já existente para as duas seleções; novo hook compartilhado `useUsuarioOptions`. Filtro por Lançamento original sincronizado com a URL; `lancamento-financeiro/` ganhou o link "Ajustes" na tabela. Testes (124/124 no total). **Com este módulo, todas as features de negócio da ordem recomendada de implementação (`arquitetura-tecnica-frontend.md`, Seção 11) estão concluídas** — restam só os itens bloqueados por S1-S5 (login/autorização/auditoria) e a Fase 6 (IA)
  - **Rodada de evolução operacional (2026-08-25 em diante)**: com todas as features de negócio da Fase 5 implementadas, o usuário trouxe feedback de uso operacional real e 14 decisões de negócio que **substituem** parte da modelagem anterior sempre que houver conflito (remoção de Liquidação Financeira e Ajuste Financeiro, conciliação via OFX, Medição de Obra nova, Faturamento Direto incorporado ao fluxo de Lançamento existente, entre outras) — ver `pendencias.md`, Seção 9, para o registro completo das 14 decisões.
    - [x] **Fase 1 — máscara de moeda universal + Categoria ordenação alfabética + Lançamento (descrição + documento/NF)**: `shared/components/form/CurrencyInput.tsx` (novo, primeiro uso de `Controller` no projeto) aplicado a todo campo de valor monetário em Real ainda vivo (`LancamentoForm`, `CompraCartaoForm`, `FaturaFecharCicloForm`, `ContratoFinanceiroForm` — só `valorContratado`, não `taxa`, percentual —, `TransferenciaInternaForm`, `ImportarMovimentacoesForm` — com `allowNegative`); `LiquidacaoForm`/`AjusteFinanceiroForm` deliberadamente fora de escopo (removidos na Sprint 3). Backend: `CategoriaRepository#findAllByOrderByNomeAsc` (ordem alfabética, não mais ordem de cadastro); `LancamentoFinanceiro` ganhou `descricao`/`documento` (opcionais, `V16`). Ver `arquitetura-tecnica-frontend.md`, Seção 5, para o padrão `CurrencyInput`/`Controller`. Testes: backend 160/160, frontend 131/131 — ver `changelog.md`
    - **Mudança de estratégia após a Fase 1 e o primeiro teste prático (decisão explícita do usuário)**: a divisão original em 8 fases pequenas e tecnicamente isoladas dificultava validar a experiência real de uso — funcionalidades do mesmo processo operacional ficavam espalhadas por fases diferentes, sem nunca existir um momento em que o processo inteiro pudesse ser testado do início ao fim. A partir daqui, a divisão passa a ser por **fluxo operacional completo da empresa**, não por semelhança técnica de código — cada entrega deve deixar um processo inteiro utilizável, mesmo que isso reduza reaproveitamento técnico entre entregas. Renomeado de "Fase N" para "Sprint N" a partir daqui, para marcar a mudança de critério:
    - [x] **Sprint 2 — Fluxo completo de Lançamentos**: `shared/components/form/AutocompleteField.tsx` (novo, segundo uso de `Controller` no projeto) substitui `SelectField` para Fornecedor/Categoria em `LancamentoForm.tsx` — busca por texto sobre as opções já carregadas + cadastro rápido embutido ("+ Criar '...'" quando o texto não corresponde a nada existente, cria e seleciona na hora, sem sair da tela). Cliente permanece `SelectField` (decisão explícita — volume baixo, autocomplete não traz ganho). Cadastro rápido de Categoria herda automaticamente o `tipo` (Despesa/Receita) já selecionado no Lançamento. Dois hooks novos em `shared/hooks/`: `useCriarFornecedorRapido`/`useCriarCategoriaRapida`. Achado real (só reproduzível com foco de janela verdadeiro, nunca em jsdom): o `DismissableLayer` do Radix Popover fechava o painel no mesmo instante em que abria, por tratar o campo (dentro de `Popover.Anchor`, não `Trigger`) como clique "de fora" — corrigido, ver `arquitetura-tecnica-frontend.md`, Seção 5. Testes: frontend 143/143 (backend sem alteração nesta sprint) — ver `changelog.md`
    - [ ] **Sprint 3 — Fluxo Financeiro**: remoção completa de Liquidação Financeira e Ajuste Financeiro; Vínculo Conciliação passa a referenciar Lançamento Financeiro diretamente (N:N); nenhum estado "Pago" nem label "Aberto" visível — só conciliado/não-conciliado; Contas a Pagar/Receber como **filtros da listagem de Lançamento já existente** (não telas novas — lançamentos ativos + não conciliados, Despesa/Receita); Conta Bancária e Cartão de Crédito sem `apelido` (mesmo fluxo operacional de gestão financeira/conciliação — decisão revisada: não pertence à Sprint 2, que é só o fluxo de cadastro de Lançamento). Conciliação continua usando o mecanismo atual (entrada manual de item de extrato) — OFX fica para a Sprint 5
    - [ ] **Sprint 4 — Obras**: Obra deixa de ser só cadastro e passa a ser o centro operacional — a própria tela de Obra (sem tela nova) evolui para mostrar, no mesmo contexto, dados cadastrais + valor contratado + valor recebido + saldo contratual + medições + faturamento direto + custos + resultado parcial. Medição de Obra (entidade nova) com o mecanismo de Faturamento Direto **embutido nela** (não antecipado para a Sprint 2 — Faturamento Direto só existe como consequência de uma Medição, nunca criado manualmente pelo usuário fora desse fluxo) e lançamentos de impostos. Regra definitiva: itens de Faturamento Direto (custos pagos por terceiro fora da conta bancária da empresa) nunca entram em Contas a Pagar nem a Receber e não são conciliados individualmente — só o valor líquido esperado da Medição entra em Contas a Receber; impostos, ao contrário, são despesa real da empresa e seguem o fluxo normal (Contas a Pagar, conciliação, movimentação bancária). Obra (data real término) e Veículo (remoção de `obraAtual` da interface) também entram aqui, por proximidade operacional (Veículo pode ser feito isoladamente a qualquer momento, sem pertencer formalmente à sprint)
    - [ ] **Sprint 5 — Conciliação Final**: importação OFX, parser definitivo, conciliação automática, melhorias finais na tela de conciliação, novo Balanço baseado na arquitetura revisada. Ainda bloqueada pela mesma limitação de sempre — precisa de uma amostra real de arquivo `.ofx` de um banco real usado pela empresa

## Fase 6
- [ ] IA

## Fase 7
- [ ] Integração bancária