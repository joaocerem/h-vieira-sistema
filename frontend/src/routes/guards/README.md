# routes/guards/

`RequireAuth` e `RequireRole` vivem aqui — deliberadamente **não implementados nesta etapa**
(`decisions.md`, decisão #41; `pendencias.md`, Seção 7, itens S1-S3). Ver
`docs/architecture/arquitetura-tecnica-frontend.md`, Seção 4 e Seção 12.
